name: Litgrid Monitor

on:
  schedule:
    - cron: '30 6 * * *'   # Kasdien 09:30 Lietuvos laiku vasarą (06:30 UTC = EEST UTC+3)
                            # Žiemą (EET UTC+2) bus 08:30 LT – GitHub Actions nepalaiko timezone
  workflow_dispatch:       # Galima paleisti rankiniu būdu

jobs:
  monitor:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - run: pip install requests

      - name: Paleisti Litgrid monitoringą
        env:
          TELEGRAM_TOKEN:   ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GMAIL_USER:       ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASS:   ${{ secrets.GMAIL_APP_PASS }}
          NOTIFY_EMAIL:     ${{ secrets.NOTIFY_EMAIL }}
        run: python monitor.py

      - name: Paleisti PAV monitoringą
        env:
          TELEGRAM_TOKEN:   ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GMAIL_USER:       ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASS:   ${{ secrets.GMAIL_APP_PASS }}
          NOTIFY_EMAIL:     ${{ secrets.NOTIFY_EMAIL }}
        run: python monitor_pav.py

      - name: Išsaugoti duomenis
        run: |
          git config user.name  "Litgrid Bot"
          git config user.email "bot@noreply.github.com"
          git add data/previous.json data/pav_previous.json
          git diff --staged --quiet || git commit -m "snapshot: $(date -u +%Y-%m-%d)"
          git push
